{% extends "admin/base_admin.html" %}
{% block title %}{{ action }} Fixture – Admin{% endblock %}

{% block admin_content %}
<div class="d-flex justify-content-between align-items-center mb-4">
    <h2 style="font-family:'Barlow Condensed',sans-serif;color:var(--lime);letter-spacing:2px">
        <i class="bi bi-calendar3 me-3"></i>{{ action }} Fixture
    </h2>
    <a href="{{ url_for('admin.matches') }}" class="btn btn-outline-secondary">
        <i class="bi bi-arrow-left me-2"></i>Back
    </a>
</div>

<div class="card">
    <div class="card-body p-4">
        <form method="POST">
            <div class="row g-3">
                <div class="col-md-5">
                    <label class="form-label">Home Team *</label>
                    <select name="home_team_id" class="form-select" required>
                        <option value="">-- Select Home Team --</option>
                        {% for team in teams %}
                        <option value="{{ team.id }}"
                            {% if match and match.home_team_id == team.id %}selected{% endif %}>
                            {{ team.name }}
                        </option>
                        {% endfor %}
                    </select>
                </div>
                <div class="col-md-2 d-flex align-items-end justify-content-center pb-1">
                    <span style="color:var(--lime);font-family:'Barlow Condensed',sans-serif;font-size:1.5rem">VS</span>
                </div>
                <div class="col-md-5">
                    <label class="form-label">Away Team *</label>
                    <select name="away_team_id" class="form-select" required>
                        <option value="">-- Select Away Team --</option>
                        {% for team in teams %}
                        <option value="{{ team.id }}"
                            {% if match and match.away_team_id == team.id %}selected{% endif %}>
                            {{ team.name }}
                        </option>
                        {% endfor %}
                    </select>
                </div>
                <div class="col-md-4">
                    <label class="form-label">Match Date & Time *</label>
                    <input type="datetime-local" name="match_date" class="form-control"
                           value="{{ match.match_date if match else '' }}" required>
                </div>
                <div class="col-md-4">
                    <label class="form-label">Venue</label>
                    <input type="text" name="venue" class="form-control"
                           value="{{ match.venue if match else '' }}" placeholder="e.g. Field A">
                </div>
                <div class="col-md-4">
                    <label class="form-label">Matchday</label>
                    <input type="number" name="matchday" class="form-control"
                           value="{{ match.matchday if match else 1 }}" min="1">
                </div>

                {% if match %}
                <!-- Result entry for existing matches -->
                <div class="col-12"><hr style="border-color:rgba(0,212,170,0.2)">
                    <h6 style="color:var(--lime)"><i class="bi bi-trophy me-2"></i>Enter Result</h6>
                </div>
                <div class="col-md-3">
                    <label class="form-label">Home Goals</label>
                    <input type="number" name="home_goals" class="form-control"
                           value="{{ match.home_goals if match.home_goals is not none else '' }}" min="0">
                </div>
                <div class="col-md-3">
                    <label class="form-label">Away Goals</label>
                    <input type="number" name="away_goals" class="form-control"
                           value="{{ match.away_goals if match.away_goals is not none else '' }}" min="0">
                </div>
                <div class="col-md-3">
                    <label class="form-label">Status</label>
                    <select name="status" class="form-select">
                        {% for s in ['scheduled','played','postponed'] %}
                        <option value="{{ s }}" {% if match.status == s %}selected{% endif %}>{{ s|capitalize }}</option>
                        {% endfor %}
                    </select>
                </div>
                <div class="col-12">
                    <small class="text-muted">
                        <i class="bi bi-info-circle me-1"></i>
                        Entering goals will automatically mark the match as "played" and update the league table.
                    </small>
                </div>
                {% endif %}

                <div class="col-12 mt-3">
                    <button type="submit" class="btn btn-primary px-5">
                        <i class="bi bi-check-circle me-2"></i>{{ action }} Fixture
                    </button>
                    <a href="{{ url_for('admin.matches') }}" class="btn btn-outline-secondary ms-2">Cancel</a>
                </div>
            </div>
        </form>
    </div>
</div>
{% endblock %}
